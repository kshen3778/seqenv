Check writable status of database download directory, else use home directory

Add a function that checks that NT is installed

Add function to take FASTQ files

Possibly: Have the outputted named with OTU names sorted in the same order as the user provided them

Possibly: Add the word cloud and heatmap generation back.