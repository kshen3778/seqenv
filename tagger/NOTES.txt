If you recompile the tagger, but don't want to see the changes in git and accidentally commit them, use:

    git update-index --assume-unchanged tagger/_tagger_swig.so
    git update-index --assume-unchanged tagger/libtagger.so
    git update-index --assume-unchanged tagger/makefile
    git update-index --assume-unchanged tagger/tagger.o




